# Codex Mobile — release build currently ships with minification disabled
# (see app/build.gradle.kts: isMinifyEnabled = false) so this file is a
# placeholder for when/if that is turned on later.
-keep class com.codex.mobile.** { *; }
