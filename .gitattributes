# The native Codex binary is 250MB+, over GitHub's 100MB hard file-size
# limit for regular git objects. It MUST be tracked with Git LFS or every
# push containing it will be rejected by GitHub.
app/src/main/jniLibs/arm64-v8a/libcodexcore.so filter=lfs diff=lfs merge=lfs -text
